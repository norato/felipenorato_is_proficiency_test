.container
  = simple_form_for @student, :validate => true do |f|
    = f.input :name
    = f.input :register_number
    = f.input :status, as: :select, collection: Status.to_a
    = f.submit class: "btn btn-success pull-right"
  = link_to t(:back), courses_path